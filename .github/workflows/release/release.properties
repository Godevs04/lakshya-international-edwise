# Release metadata — updated by Actions → Version Release
# Edit version.prefix manually; other fields are managed by CI.

version.prefix=v
version.last=0.0.0
version.current=0.1.0
version.new=0.1.0
version.bump=none
release.tag=v0.1.0
release.date=
release.ref=
